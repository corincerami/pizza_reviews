<div>
<ul>
<% @pizzerias.each do |pizzeria| %>
  <div class = "pizzeria"><li><%= pizzeria.name %>
  <%= link_to("Delete pizzeria", pizzeria_path(pizzeria), method: :delete, confirm: true, class: "button alert round") %> %></li></div>
<% end %>
</ul>
</div>
